# React 组件数组（简单版）

## 1. **基本用法**
```jsx
// 最简单的例子：渲染列表
function ShoppingList() {
  const items = ['苹果', '香蕉', '橙子'];
  
  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{item}</li>
      ))}
    </ul>
  );
}
// 显示：
// • 苹果
// • 香蕉
// • 橙子
```

## 2. **Key 很重要！**
```jsx
// 错误示范
function WrongList() {
  const fruits = ['苹果', '香蕉'];
  return (
    <div>
      {fruits.map(fruit => <span>{fruit}</span>)} {/* ❌ 缺少 key */}
    </div>
  );
}

// 正确做法
function RightList() {
  const fruits = ['苹果', '香蕉'];
  return (
    <div>
      {fruits.map((fruit, index) => (
        <span key={index}>{fruit}</span>  {/* ✅ 有 key */}
      ))}
    </div>
  );
}
```

## 3. **实际例子**
```jsx
// 渲染用户列表
function UserList() {
  const users = [
    { id: 1, name: '小明', age: 20 },
    { id: 2, name: '小红', age: 22 },
    { id: 3, name: '小刚', age: 25 }
  ];
  
  return (
    <div>
      <h2>用户列表</h2>
      {users.map(user => (
        <div key={user.id} className="user-card">
          <h3>{user.name}</h3>
          <p>年龄: {user.age}</p>
        </div>
      ))}
    </div>
  );
}
```

## 4. **简单技巧**

### 过滤列表
```jsx
function AdultUsers() {
  const users = [
    { id: 1, name: '小明', age: 17 },
    { id: 2, name: '小红', age: 22 },
    { id: 3, name: '小刚', age: 25 }
  ];
  
  // 只显示成年人
  const adults = users.filter(user => user.age >= 18);
  
  return (
    <div>
      {adults.map(user => (
        <div key={user.id}>{user.name} ({user.age}岁)</div>
      ))}
    </div>
  );
}
```

### 条件显示
```jsx
function TodoList() {
  const todos = [
    { id: 1, text: '学习 React', done: true },
    { id: 2, text: '买 groceries', done: false },
    { id: 3, text: '写作业', done: false }
  ];
  
  return (
    <div>
      <h3>待办事项</h3>
      {todos.map(todo => (
        <div key={todo.id}>
          <input type="checkbox" checked={todo.done} />
          <span style={{ 
            textDecoration: todo.done ? 'line-through' : 'none'
          }}>
            {todo.text}
          </span>
        </div>
      ))}
    </div>
  );
}
```

## 5. **常见问题**

### 问题1：列表为空时
```jsx
function ProductList({ products }) {
  if (products.length === 0) {
    return <p>暂无商品</p>;  // ✅ 好的做法
  }
  
  return (
    <div>
      {products.map(product => (
        <div key={product.id}>{product.name}</div>
      ))}
    </div>
  );
}
```

### 问题2：重复的 key
```jsx
// ❌ 错误：重复的 key
const items = ['苹果', '香蕉', '苹果'];
{items.map((item, index) => (
  <div key={item}>{item}</div>  // 两个"苹果"会冲突！
))}

// ✅ 正确：加上索引或其他唯一值
{items.map((item, index) => (
  <div key={`${item}-${index}`}>{item}</div>
))}
```

## 6. **实用模板**
```jsx
// 常用结构
function MyComponent() {
  // 1. 准备数据
  const items = [
    { id: 1, title: '标题1', content: '内容1' },
    { id: 2, title: '标题2', content: '内容2' },
  ];
  
  // 2. 检查是否为空
  if (items.length === 0) {
    return <p>没有数据</p>;
  }
  
  // 3. 渲染列表
  return (
    <div className="container">
      <h2>我的列表</h2>
      <div className="list">
        {items.map(item => (
          <div key={item.id} className="item">
            <h3>{item.title}</h3>
            <p>{item.content}</p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

## 📝 **记住三条规则：**
1. **必须加 `key`**：`map` 里的每个元素都要有 `key` 属性
2. **key 要唯一**：同一个列表里不能有重复的 key
3. **用 index 要小心**：如果列表会变化（增删改），最好用数据里的 id

## 🎯 **初学者练习：**
```jsx
// 试试这个练习
function Exercise() {
  const books = [
    { id: 1, title: 'React 入门', author: '张老师', price: 59 },
    { id: 2, title: 'JavaScript 基础', author: '李老师', price: 49 },
    { id: 3, title: 'CSS 技巧', author: '王老师', price: 39 }
  ];
  
  // 你的任务：渲染这个书籍列表
  // 1. 显示书名和作者
  // 2. 价格大于45的书显示为红色
  // 3. 如果没有书，显示"暂无书籍"
  
  return (
    <div>
      {/* 在这里写代码 */}
    </div>
  );
}
```

这样就够用了！先从简单的开始，等熟悉了再学更高级的用法。