在 React 中，条件渲染有多种方法。以下是常用的几种方式：

## 1. **if 语句（组件级别）**
```jsx
function UserGreeting({ isLoggedIn }) {
  if (isLoggedIn) {
    return <h1>欢迎回来!</h1>;
  }
  return <h1>请先登录。</h1>;
}

// 用于提前返回
function Component({ data }) {
  if (!data) {
    return <Loading />;
  }
  
  if (data.error) {
    return <Error message={data.error} />;
  }
  
  return <MainContent data={data} />;
}
```

## 2. **三元运算符（内联条件）**
```jsx
function UserStatus({ isOnline }) {
  return (
    <div>
      <h1>用户状态</h1>
      {isOnline ? (
        <span style={{ color: 'green' }}>在线</span>
      ) : (
        <span style={{ color: 'gray' }}>离线</span>
      )}
    </div>
  );
}

// 嵌套三元（谨慎使用）
function PriorityLabel({ priority }) {
  return (
    <span>
      {priority === 'high' ? '🔥' 
       : priority === 'medium' ? '⚠️' 
       : '📌'}
    </span>
  );
}
```

## 3. **逻辑与运算符（&&）**
```jsx
function Notification({ count }) {
  return (
    <div>
      <h1>通知</h1>
      {count > 0 && (
        <div className="badge">
          有 {count} 条未读通知
        </div>
      )}
    </div>
  );
}

// 多个条件
function Dashboard({ user, settings }) {
  return (
    <div>
      {user && user.isAdmin && (
        <AdminPanel />
      )}
      {settings.showAnalytics && (
        <AnalyticsDashboard />
      )}
    </div>
  );
}
```

## 4. **立即执行函数（IIFE）**
```jsx
function ComplexComponent({ type, data }) {
  return (
    <div>
      <Header />
      {
        (() => {
          switch(type) {
            case 'image':
              return <ImageGallery images={data} />;
            case 'video':
              return <VideoPlayer src={data} />;
            case 'text':
              return <Article content={data} />;
            default:
              return <DefaultContent />;
          }
        })()
      }
      <Footer />
    </div>
  );
}
```

## 5. **提取为独立函数/组件**
```jsx
// 提取渲染逻辑
function renderContent(userType) {
  switch(userType) {
    case 'admin':
      return <AdminDashboard />;
    case 'user':
      return <UserDashboard />;
    case 'guest':
      return <GuestView />;
    default:
      return <LoginPrompt />;
  }
}

function App({ userType }) {
  return (
    <div className="app">
      <Header />
      {renderContent(userType)}
      <Footer />
    </div>
  );
}

// 或提取为独立组件
function UserContent({ userType }) {
  switch(userType) {
    case 'admin': return <AdminDashboard />;
    case 'user': return <UserDashboard />;
    default: return <LoginPrompt />;
  }
}
```

## 6. **使用对象映射**
```jsx
// 适合固定的键值映射
const COMPONENT_MAP = {
  success: <SuccessIcon />,
  error: <ErrorIcon />,
  warning: <WarningIcon />,
  info: <InfoIcon />
};

function StatusIndicator({ status }) {
  return (
    <div>
      {COMPONENT_MAP[status] || <DefaultIcon />}
    </div>
  );
}

// 组件映射
const COMPONENTS = {
  button: ButtonComponent,
  input: InputComponent,
  select: SelectComponent,
  textarea: TextareaComponent
};

function DynamicForm({ fields }) {
  return fields.map((field) => {
    const Component = COMPONENTS[field.type] || DefaultComponent;
    return <Component key={field.id} {...field} />;
  });
}
```

## 7. **使用 Hooks 封装条件逻辑**
```jsx
// 自定义 Hook
function useConditionalRender(condition, trueComponent, falseComponent = null) {
  return condition ? trueComponent : falseComponent;
}

// 使用
function ProductPage({ product }) {
  const discountDisplay = useConditionalRender(
    product.discount > 0,
    <DiscountBadge discount={product.discount} />,
    null
  );
  
  return (
    <div>
      <ProductImage src={product.image} />
      {discountDisplay}
      <ProductPrice price={product.price} />
    </div>
  );
}
```

## 8. **React 18+ Suspense 处理**
```jsx
import { Suspense } from 'react';

function App({ isPremium }) {
  return (
    <div>
      <Header />
      <Suspense fallback={<Loading />}>
        {isPremium ? (
          <PremiumContent />
        ) : (
          <FreeContent />
        )}
      </Suspense>
    </div>
  );
}
```

## 📊 **最佳实践总结**

| 场景 | 推荐方法 | 示例 |
|------|----------|------|
| **组件整体条件** | `if` 语句提前返回 | `if (!data) return <Loading />` |
| **简单二选一** | 三元运算符 | `{isActive ? '激活' : '未激活'}` |
| **条件显示元素** | 逻辑与 `&&` | `{hasError && <ErrorMessage />}` |
| **多条件分支** | `switch` 或对象映射 | `switch(type) { case 'A': ... }` |
| **复杂逻辑** | 提取为函数/组件 | `renderUserContent(user)` |
| **列表渲染** | `filter` + `map` | `items.filter(i => i.active).map()` |

## ⚠️ **注意事项**

```jsx
// ❌ 错误：数字 0 会被渲染为 false
function Counter({ count }) {
  return (
    <div>
      {count && <span>计数: {count}</span>}
    </div>
  );
  // count = 0 时，会渲染 0，而不是不渲染
}

// ✅ 正确：明确判断
function Counter({ count }) {
  return (
    <div>
      {count !== undefined && <span>计数: {count}</span>}
    </div>
  );
}
```

## 🎯 **选择建议**

1. **简单条件**：使用 `&&` 或三元运算符
2. **多个互斥分支**：使用 `if-else` 或 `switch`
3. **复杂渲染逻辑**：提取为子组件或函数
4. **性能关键**：避免在渲染函数中创建新函数
5. **可读性优先**：选择最清晰表达意图的方式

根据具体场景选择最合适的方法，保持代码可读性和维护性是最重要的！