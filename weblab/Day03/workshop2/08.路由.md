# 路由（Routing）简单解释

## 1. **什么是路由？**

想象一个网站像一本书：
- **不同页面 = 书的章节**
- **路由 = 目录/页码**，告诉你去哪一页

```jsx
// 没有路由：只有一个页面
网站：首页（永远是这个）

// 有路由：多个页面
网站：
- /          → 首页
- /about     → 关于页  
- /contact   → 联系页
- /products  → 产品页
```

## 2. **为什么需要路由？**

### ❌ 没有路由的问题
```jsx
// 只能这样切换内容
function App() {
  const [page, setPage] = useState('home');
  
  if (page === 'home') return <HomePage />;
  if (page === 'about') return <AboutPage />;
  // ...
  
  return <button onClick={() => setPage('about')}>去关于页</button>;
}
```
**问题**：
1. 不能收藏页面（永远是同一个网址）
2. 不能后退/前进
3. 不能分享链接

### ✅ 有路由的好处
```jsx
// 浏览器地址栏：
https://example.com/           ← 首页
https://example.com/about      ← 关于页
https://example.com/products   ← 产品页

// 可以：
1. 收藏任意页面 ✓
2. 浏览器后退前进 ✓  
3. 分享特定页面链接 ✓
```

## 3. **基本路由代码（React Router）**

```jsx
// 安装：npm install react-router-dom
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      {/* 1. 导航链接 */}
      <nav>
        <Link to="/">首页</Link>
        <Link to="/about">关于</Link>
        <Link to="/contact">联系</Link>
      </nav>
      
      {/* 2. 路由配置 */}
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/about" element={<AboutPage />} />
        <Route path="/contact" element={<ContactPage />} />
      </Routes>
    </BrowserRouter>
  );
}

// 页面组件
function HomePage() { return <h1>首页</h1>; }
function AboutPage() { return <h1>关于我们</h1>; }
function ContactPage() { return <h1>联系我们</h1>; }
```

## 4. **核心概念**

### ① **路径匹配**
```jsx
<Routes>
  <Route path="/" element={<首页 />} />        // 精确匹配 /
  <Route path="/about" element={<关于页 />} /> // 精确匹配 /about
  <Route path="/user/:id" element={<用户页 />} /> // 动态路由
</Routes>
```

### ② **导航**
```jsx
// 方法1：Link 组件（推荐）
<Link to="/about">去关于页</Link>

// 方法2：编程式导航
import { useNavigate } from 'react-router-dom';

function MyComponent() {
  const navigate = useNavigate();
  
  const goToAbout = () => {
    navigate('/about');  // 跳转到 /about
  };
  
  return <button onClick={goToAbout}>去关于页</button>;
}
```

### ③ **嵌套路由**
```jsx
// 父路由
<Route path="/dashboard" element={<Dashboard />}>
  {/* 子路由 */}
  <Route path="profile" element={<Profile />} />      // /dashboard/profile
  <Route path="settings" element={<Settings />} />    // /dashboard/settings
</Route>

// Dashboard 组件中放 outlet
function Dashboard() {
  return (
    <div>
      <h1>控制面板</h1>
      <nav>
        <Link to="profile">个人资料</Link>
        <Link to="settings">设置</Link>
      </nav>
      
      {/* 子页面在这里显示 */}
      <Outlet />
    </div>
  );
}
```

## 5. **常见路由模式**

```jsx
// 1. 普通网站路由
/dashboard        控制面板
/dashboard/users  用户管理
/dashboard/settings 设置

// 2. 电商网站路由  
/products         产品列表
/products/123     产品详情
/cart             购物车
/checkout         结算

// 3. 动态路由
/user/:id         用户主页（:id 是变量）
/blog/:year/:month/:slug  博客文章

// 4. 404 页面
<Route path="*" element={<NotFoundPage />} />
```

## 6. **实际例子**

```jsx
// 简单的博客网站路由
function BlogApp() {
  return (
    <BrowserRouter>
      <Header />
      
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/posts" element={<PostList />} />
        <Route path="/posts/:id" element={<PostDetail />} />
        <Route path="/about" element={<AboutPage />} />
        <Route path="/contact" element={<ContactPage />} />
        <Route path="/login" element={<LoginPage />} />
        
        {/* 404 页面 */}
        <Route path="*" element={<NotFound />} />
      </Routes>
      
      <Footer />
    </BrowserRouter>
  );
}

// 动态路由例子
function PostDetail() {
  const { id } = useParams(); // 获取路由参数
  
  return <h1>文章 ID: {id}</h1>;
  // 访问 /posts/123 显示：文章 ID: 123
  // 访问 /posts/456 显示：文章 ID: 456
}
```

## 7. **初学者建议**

```jsx
// 先学会这些就够了：
function MyFirstRouter() {
  return (
    <BrowserRouter>
      {/* 1. 链接 */}
      <nav>
        <Link to="/">首页</Link>
        <Link to="/about">关于</Link>
      </nav>
      
      {/* 2. 路由配置 */}
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  );
}

// 3. 页面组件
function Home() {
  return <h1>欢迎来到首页！</h1>;
}

function About() {
  return <h1>关于我们页面</h1>;
}
```

## 🎯 **一句话总结：**
**路由就是让一个网址（URL）对应一个页面（组件），实现多页面网站效果。**

## 📝 **记住这几点：**
1. **`<Link to="路径">`**：点击跳转（类似 `<a href>`）
2. **`<Route path="路径" element={组件} />`**：配置哪个路径显示哪个组件
3. **`<Routes>` 包住所有 `<Route>`**
4. **`<BrowserRouter>` 包住整个应用**

路由让单页面应用（SPA）有了多页面网站的效果！