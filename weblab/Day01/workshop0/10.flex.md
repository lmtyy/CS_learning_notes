# Flexbox 简单入门

## 什么是 Flexbox？

Flexbox 就像**智能的排列盒子**，让里面的元素自动排列、对齐和分布空间。

## 最基本的用法

```css
.container {
  display: flex; /* 开启 flex 布局 */
}
```

只要这一行代码，**容器里的子元素**就会自动变成水平排列！

## 最常用的几个属性

### 1. **水平排列方向**
```css
.container {
  display: flex;
  flex-direction: row;    /* 从左到右（默认） */
  flex-direction: column; /* 从上到下 */
}
```

### 2. **水平对齐**
```css
.container {
  display: flex;
  justify-content: center;    /* 居中 */
  justify-content: space-between; /* 两端对齐 */
}
```

### 3. **垂直对齐**
```css
.container {
  display: flex;
  align-items: center; /* 垂直居中 */
}
```

### 4. **自动换行**
```css
.container {
  display: flex;
  flex-wrap: wrap; /* 空间不够时自动换行 */
}
```

## 实际例子

### 水平居中（超常用！）
```css
.container {
  display: flex;
  justify-content: center; /* 水平居中 */
  align-items: center;     /* 垂直居中 */
}
```

### 等分布局
```css
.container {
  display: flex;
}

.item {
  flex: 1; /* 所有项目等宽 */
}
```

### 导航菜单
```css
.nav {
  display: flex;
  justify-content: space-between; /* 左边logo，右边菜单 */
}
```

## 记住这3个最常用的：

1. `display: flex` - 开启 flex
2. `justify-content` - 控制水平对齐  
3. `align-items` - 控制垂直对齐

**一句话理解：Flexbox 就是让元素听话地排队和站队！** 🎯